from fasthtml.common import * # type: ignore
# from fasthtml.common import (Form, Fieldset, Label, Input, Button, Html, Head, Body, Div, P, Title, Titled, A, Link)
import re

app, rt = fast_app(static_path="static") # type: ignore

